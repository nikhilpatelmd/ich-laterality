---
title: "Ordinal Regression - Statistical Rethinking"
code-fold: true
---

```{r setup, warning=FALSE, message=FALSE}

library(tidyverse)
library(brms)
library(cmdstanr)
library(here)

# seeds
set.seed(160)
BAYES_SEED <- 160

# Bayes
options(
  mc.cores = parallel::detectCores(),
  brms.backend = "cmdstanr"
)

```


```{r import data}

data <- read_rds(here("data", "all.rds"), refhook = NULL)


```